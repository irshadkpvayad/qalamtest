<div class="glass glass-panel text-center fade-in" style="margin-bottom:3rem; padding:4rem 2rem;">
  <h1 style="font-size:2.5rem; margin-bottom:1rem;">തിരയൽ ഫലങ്ങൾ</h1>
  <p style="color:var(--text-secondary);">"<%= query %>" എന്നതിനായുള്ള ഫലങ്ങൾ</p>
</div>

<div class="grid-layout">
  <div>
    <% if (posts.length === 0) { %>
      <div class="glass glass-panel text-center fade-in stagger-1">
        <p>ഫലങ്ങളൊന്നും കണ്ടെത്തിയില്ല.</p>
      </div>
    <% } else { %>
      <div class="post-grid fade-in stagger-1">
        <% posts.forEach((post) => { %>
          <%- include('partials/post-card', { post }) %>
        <% }) %>
      </div>
    <% } %>
  </div>
</div>
